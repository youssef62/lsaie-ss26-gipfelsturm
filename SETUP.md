
# Setup Instructions

- Copy the configuration file to your home directory:
    ```bash
    cp /users/course_00197/scratch/lsaie-ss26-gipfelsturm/alps3.toml ~/.edf/
    ```
- Change the necessary paths in `lauch.sh` (`WORKDIR`) and `test-infra.sbatch` (`WORKDIR`) and `alps3.toml`(`workdir`).

